package com.edsuuu.utilidades;

// Repare no 'package' acima. Esta classe está dentro do sub-pacote 'utilidades'.
// Pacotes (Packages) servem para organizar o código em pastas lógicas, evitando conflitos de nomes
// e ajudando a agrupar classes que têm responsabilidades semelhantes.
public class Calculadora {
    
    // Método público que poderá ser acessado por classes de outros pacotes
    public static int somar(int a, int b) {
        return a + b;
    }
    
}
